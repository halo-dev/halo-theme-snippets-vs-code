{
    "$linkId": {
		"prefix": "$l.id",
		"body": [
            "${link.id?c}"
		],
		"description": "${link.id?c}",
		"scope": "text.html.ftl"
    },
    "$linkName": {
		"prefix": "$l.name",
		"body": [
            "${link.name!}"
		],
		"description": "${link.name!}",
		"scope": "text.html.ftl"
    },
    "$linkUrl": {
		"prefix": "$l.url",
		"body": [
            "${link.url!}"
		],
		"description": "${link.url!}",
		"scope": "text.html.ftl"
    },
    "$linkLogo": {
		"prefix": "$l.logo",
		"body": [
            "${link.logo!}"
		],
		"description": "${link.logo!}",
		"scope": "text.html.ftl"
    },
    "$linkDescription": {
		"prefix": "$l.description",
		"body": [
            "${link.description!}"
		],
		"description": "${link.description!}",
		"scope": "text.html.ftl"
    },
    "$linkTeam": {
		"prefix": "$l.team",
		"body": [
            "${link.team!}"
		],
		"description": "${link.team!}",
		"scope": "text.html.ftl"
    },
    "$linkCreateTime": {
		"prefix": "$l.createTime",
		"body": [
            "${link.createTime?string('yyyy-MM-dd')}"
		],
		"description": "${link.createTime?string('yyyy-MM-dd')}",
		"scope": "text.html.ftl"
    },
    "$linkUpdateTime": {
		"prefix": "$l.updateTime",
		"body": [
            "${link.updateTime?string('yyyy-MM-dd')}"
		],
		"description": "${link.updateTime?string('yyyy-MM-dd')}",
		"scope": "text.html.ftl"
    }
}