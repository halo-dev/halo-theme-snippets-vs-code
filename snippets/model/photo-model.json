{
    "$photoId": {
		"prefix": "$ph.id",
		"body": [
            "${photo.id?c}"
		],
		"description": "${photo.id?c}",
		"scope": "text.html.ftl"
    },
    "$photoName": {
		"prefix": "$ph.name",
		"body": [
            "${photo.name!}"
		],
		"description": "${photo.name!}",
		"scope": "text.html.ftl"
    },
    "$photoDescription": {
		"prefix": "$ph.description",
		"body": [
            "${photo.description!}"
		],
		"description": "${photo.description!}",
		"scope": "text.html.ftl"
    },
    "$photoTakeTime": {
		"prefix": "$ph.takeTime",
		"body": [
            "${photo.takeTime!}"
		],
		"description": "${photo.takeTime!}",
		"scope": "text.html.ftl"
    },
    "$photoLocation": {
		"prefix": "$ph.location",
		"body": [
            "${photo.location!}"
		],
		"description": "${photo.location!}",
		"scope": "text.html.ftl"
    },
    "$photoThumbnail": {
		"prefix": "$ph.thumbnail",
		"body": [
            "${photo.thumbnail!}"
		],
		"description": "${photo.thumbnail!}",
		"scope": "text.html.ftl"
    },
    "$photoUrl": {
		"prefix": "$ph.url",
		"body": [
            "${photo.url!}"
		],
		"description": "${photo.url!}",
		"scope": "text.html.ftl"
    },
    "$photoTeam": {
		"prefix": "$ph.team",
		"body": [
            "${photo.team!}"
		],
		"description": "${photo.team!}",
		"scope": "text.html.ftl"
    },
    "$photoCreateTime": {
		"prefix": "$ph.createTime",
		"body": [
            "${photo.createTime?string('yyyy-MM-dd')}"
		],
		"description": "${photo.createTime?string('yyyy-MM-dd')}",
		"scope": "text.html.ftl"
    },
    "$photoUpdateTime": {
		"prefix": "$ph.updateTime",
		"body": [
            "${photo.updateTime?string('yyyy-MM-dd')}"
		],
		"description": "${photo.updateTime?string('yyyy-MM-dd')}",
		"scope": "text.html.ftl"
    }
}